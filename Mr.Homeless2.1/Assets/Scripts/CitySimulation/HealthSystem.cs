using UnityEngine;

public class HealthSystem : MonoBehaviour
{
    /*
    public void Simulate(CityData city)
    {
        float health = city.healthcareLevel * 10f - city.pollutionRate * 0.3f;
        city.healthRate = Mathf.Clamp(health, 0f, 100f);

        if (city.healthRate < 40f)
        {
            city.happinessRate -= 2f;
        }

        city.happinessRate = Mathf.Clamp(city.happinessRate, 0f, 100f);
    }*/
}
